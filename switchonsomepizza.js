function switchOnPizza(exp) {
  var answer = "";
  // Only change code below this line
   if (exp == "hungry and thirsty"){
     return "Let's get pizza and drinks.";
   }
   else if (exp == "hungry"){
     return "Let's get pizza.";
   }
   else if (exp == "thirsty"){
     return "Let's get drinks.";
   }
}
