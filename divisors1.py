def divisors(integer):
  ## case find all factors
  
  for i in integers:
    count = 1
    if i % (count == 0):
      arr = []
      push.arr[]
      count = count + 1
        if count == integer:
          break;
    else:
      count = count + 1
      if count == integer:
        break
  
    print arr
    
  ## case prime
  else:
    print(str(integer) + " is prime")
  
  return

#divisors(15) #[3, 5];
#divisors(12) #[2, 3, 4, 6];
divisors(13) # "13 is prime";