def is_kiss(words):
        a = words.split(" ")
        arr = []
        b = len(a)
        for i in a:
          c = arr.append(len(i))
        x = max(arr)
        if b >= x:
          return "Good work Joe!"
        else:
          return "Keep It Simple Stupid"
		  